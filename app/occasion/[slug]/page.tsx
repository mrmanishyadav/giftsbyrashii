import{CatalogPage}from'@/components/catalog-page';export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const title=`${slug.replaceAll('-',' ').replace(/\b\w/g,c=>c.toUpperCase())} gifts`;return <CatalogPage title={title} description={`Memorable surprises chosen for a beautiful ${slug.replaceAll('-',' ')}.`} filter={{kind:'occasion',value:slug}}/>}
