import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';export async function POST(request:Request){const form=await request.formData();const password=String(form.get('password')??'');const confirm=String(form.get('confirmPassword')??'');if(password.length<8||password!==confirm)return NextResponse.redirect(new URL('/reset-password?error=invalid',request.url),303);const supabase=await createClient();if(supabase)await supabase.auth.updateUser({password});return NextResponse.redirect(new URL('/account',request.url),303);}
