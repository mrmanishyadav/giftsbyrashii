import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';export async function POST(request:Request){const form=await request.formData();const email=String(form.get('email')??'');const supabase=await createClient();if(supabase&&email)await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${process.env.NEXT_PUBLIC_SITE_URL??'http://localhost:3000'}/reset-password`});return NextResponse.redirect(new URL('/login?notice=reset-link-sent',request.url),303);}
