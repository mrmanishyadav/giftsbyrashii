import{NextResponse}from'next/server';import{corporateSchema}from'@/lib/validations';import{createAdminClient}from'@/lib/supabase/admin';export async function POST(request:Request){const parsed=corporateSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:'Please complete all enquiry details.'},{status:400});const admin=createAdminClient();if(!admin)return NextResponse.json({error:'Enquiry storage is ready but Supabase is not configured.'},{status:503});const d=parsed.data;const{error}=await admin.from('corporate_enquiries').insert({company:d.company,contact_person:d.contactPerson,email:d.email,phone:d.phone,quantity:d.quantity,budget:d.budget,occasion:d.occasion,delivery_location:d.deliveryLocation,requirements:d.requirements});return error?NextResponse.json({error:'Enquiry could not be saved.'},{status:400}):NextResponse.json({ok:true});}
