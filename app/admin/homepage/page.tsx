import Link from'next/link';import{HomepageManager}from'@/components/homepage-manager';import{createClient}from'@/lib/supabase/server';export default async function Page(){const supabase=await createClient();if(!supabase)return null;const{data}=await supabase!.from('homepage_sections').select('id,section_type,title,is_enabled,sort_order').order('sort_order');return <div className="admin-page"><div className="admin-title"><div><span>STOREFRONT CMS</span><h1>Homepage builder</h1><p>Reorder, schedule and publish without changing source code.</p></div><Link href="/" className="text-link">View storefront →</Link></div><HomepageManager initial={data??[]}/></div>}
