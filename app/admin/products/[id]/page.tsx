import{notFound}from'next/navigation';import{AdminProductForm}from'@/components/admin-product-form';import{createClient}from'@/lib/supabase/server';export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const supabase=await createClient();if(!supabase)return null;const{data}=await supabase!.from('products').select('*').eq('id',id).maybeSingle();if(!data)notFound();return <div className="admin-page"><div className="admin-title"><div><span>{data.sku}</span><h1>Edit {data.name}</h1><p>Updates appear everywhere this product is related.</p></div></div><AdminProductForm initial={data}/></div>}
