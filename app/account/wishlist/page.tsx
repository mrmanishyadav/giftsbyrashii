'use client';import Link from'next/link';import{Heart}from'lucide-react';import{useCart}from'@/components/cart-provider';export default function Page(){const{wishlist}=useCart();return <><div className="account-heading"><span className="eyebrow">SAVED WITH LOVE</span><h1>Your wishlist</h1></div>{wishlist.length?<div className="account-panel"><p>{wishlist.length} gift{wishlist.length!==1?'s':''} saved on this device. Sign-in sync is enabled when Supabase is configured.</p><Link href="/shop" className="button button-primary">Browse saved collection</Link></div>:<div className="mini-empty"><Heart/><p>Tap the heart on a gift to save it here.</p><Link href="/shop" className="button button-primary">Discover gifts</Link></div>}</>}
