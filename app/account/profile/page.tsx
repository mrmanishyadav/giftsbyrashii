import{requireUser}from'@/lib/auth';export const metadata={title:'Profile'};export default async function Page(){const user=await requireUser();return <><div className="account-heading"><span className="eyebrow">PERSONAL DETAILS</span><h1>Your profile</h1></div><form className="profile-form"><label className="field"><span>Full name</span><input defaultValue={user.user_metadata.full_name??''}/></label><label className="field"><span>Email</span><input value={user.email??''} disabled/></label><label className="field"><span>Mobile</span><input defaultValue={user.user_metadata.mobile??''}/></label><button className="button button-primary" type="button">Save changes</button></form></>}
