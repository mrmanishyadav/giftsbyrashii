import{AddressManager}from'@/components/address-manager';import{requireUser}from'@/lib/auth';import{createClient}from'@/lib/supabase/server';export const metadata={title:'Addresses'};export default async function Page(){const user=await requireUser();const supabase=await createClient();const{data}=await supabase!.from('addresses').select('id,name,mobile,line1,city,state,postal_code,is_default').eq('user_id',user.id).order('is_default',{ascending:false});return <AddressManager initial={data??[]}/>}
