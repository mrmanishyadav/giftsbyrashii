import type{MetadataRoute}from'next';import{getProducts}from'@/lib/catalog';export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL??'http://localhost:3000';const products=await getProducts();const staticRoutes=['','/shop','/hamper-builder','/corporate-gifting','/about','/contact','/login','/signup'];return[...staticRoutes.map(url=>({url:`${base}${url}`,lastModified:new Date(),changeFrequency:url===''?'daily' as const:'weekly' as const,priority:url===''?1:.7})),...products.map(p=>({url:`${base}/product/${p.slug}`,lastModified:new Date(),changeFrequency:'weekly' as const,priority:.8}))]}
