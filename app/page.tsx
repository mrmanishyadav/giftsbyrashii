import{HomepageSections}from'@/components/homepage-sections';import{SiteFooter,SiteHeader}from'@/components/site-shell';import{getProducts}from'@/lib/catalog';import{demoHomepageSections}from'@/lib/demo-data';import{createClient}from'@/lib/supabase/server';import type{HomepageSection}from'@/lib/types';
const defaultSections:HomepageSection[]=[...demoHomepageSections,{id:'occasions',type:'occasion_collection',title:'Gifts for every beautiful reason',enabled:true,sortOrder:4,config:{}},{id:'prices',type:'price_collection',title:'Find their joy, your way',enabled:true,sortOrder:5,config:{}},{id:'hamper',type:'custom_hamper',title:'A hamper as unique as they are.',enabled:true,sortOrder:6,config:{}},{id:'corporate',type:'corporate_gifting',title:'Make work feel a little more wonderful.',enabled:true,sortOrder:7,config:{}},{id:'testimonials',type:'testimonials',title:'Notes that made us smile',enabled:true,sortOrder:8,config:{}},{id:'usp',type:'usp',title:'Why GiftMitra',enabled:true,sortOrder:9,config:{}},{id:'newsletter',type:'newsletter',title:'Be the first to know what’s worth gifting.',enabled:true,sortOrder:10,config:{}}];
export default async function Home(){const[products,supabase]=await Promise.all([getProducts(),createClient()]);let sections=defaultSections;if(supabase){const{data}=await supabase.from('homepage_sections').select('*').eq('is_enabled',true).order('sort_order');if(data?.length)sections=data.map((r):HomepageSection=>({id:r.id,type:r.section_type,title:r.title??'',subtitle:r.subtitle??undefined,enabled:r.is_enabled,sortOrder:r.sort_order,config:(r.configuration??{}) as Record<string,unknown>}));}return <main><SiteHeader/><HomepageSections sections={sections} products={products}/><SiteFooter/></main>}
