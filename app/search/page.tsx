import{CatalogPage}from'@/components/catalog-page';export const metadata={title:'Search'};export default async function Page({searchParams}:{searchParams:Promise<{q?:string}>}){const{q}=await searchParams;return <CatalogPage title={q?`Search results for “${q}”`:'Search GiftMitra'} description={q?'The closest thoughtful matches from our collection.':'Try a person, occasion or gift idea.'} query={q}/>}
