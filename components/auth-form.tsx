'use client';
import Link from'next/link';import{Gift,LoaderCircle}from'lucide-react';import{useState}from'react';import{useRouter}from'next/navigation';
export function AuthForm({mode}:{mode:'login'|'signup'}){const[loading,setLoading]=useState(false);const[error,setError]=useState('');const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const form=new FormData(e.currentTarget);const payload=Object.fromEntries(form.entries());const response=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const data=await response.json() as {error?:string};setLoading(false);if(!response.ok){setError(data.error??'Please try again.');return}router.push(mode==='login'?'/account':'/login?notice=check-email');router.refresh()}return <div className="auth-wrap"><section className="auth-aside"><span><Gift/> GiftMitra</span><blockquote>“The best gifts don’t just say something. They make someone feel seen.”</blockquote><div>Thoughtful gifting · Secure checkout · Made in India</div></section><main className="auth-main"><Link href="/" className="back-link">← Back to GiftMitra</Link><div className="auth-card"><span className="eyebrow">{mode==='login'?'WELCOME BACK':'JOIN THE JOY'}</span><h1>{mode==='login'?'Your thoughtful corner awaits.':'Make gifting feel effortless.'}</h1><p>{mode==='login'?'Sign in to see orders, saved gifts and delivery updates.':'Create an account to save wishlists, addresses and hamper ideas.'}</p><form onSubmit={submit}>{mode==='signup'&&<><label className="field"><span>Full name</span><input name="fullName" required autoComplete="name"/></label><label className="field"><span>Mobile number</span><input name="mobile" required autoComplete="tel"/></label><label className="field"><span>Email</span><input name="email" type="email" required autoComplete="email"/></label></>}{mode==='login'&&<label className="field"><span>Email or mobile</span><input name="identifier" required autoComplete="username"/></label>}<label className="field"><span>Password</span><input name="password" type="password" required minLength={8} autoComplete={mode==='login'?'current-password':'new-password'}/></label>{mode==='signup'&&<label className="field"><span>Confirm password</span><input name="confirmPassword" type="password" required minLength={8} autoComplete="new-password"/></label>}{mode==='login'&&<Link href="/forgot-password" className="forgot">Forgot password?</Link>}{error&&<p className="form-error" role="alert">{error}</p>}<button className="button button-primary" disabled={loading}>{loading?<LoaderCircle className="spin"/>:mode==='login'?'Sign in securely':'Create my account'} →</button></form><p className="auth-switch">{mode==='login'?'New to GiftMitra?':'Already have an account?'} <Link href={mode==='login'?'/signup':'/login'}>{mode==='login'?'Create an account':'Sign in'}</Link></p></div></main></div>}
