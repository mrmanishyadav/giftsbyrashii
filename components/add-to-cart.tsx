'use client';
import{useState}from'react';import{Heart,Minus,Plus,ShoppingBag,Zap}from'lucide-react';import type{Product}from'@/lib/types';import{useCart}from'./cart-provider';import{useRouter}from'next/navigation';
export function AddToCart({product}:{product:Product}){const[quantity,setQuantity]=useState(1);const[name,setName]=useState('');const{addProduct,toggleWishlist,wishlist}=useCart();const router=useRouter();const personalization=product.personalized&&name?{recipientName:name}:undefined;function add(){addProduct(product,quantity,personalization)}return <div className="purchase-actions">{product.personalized&&<label className="field"><span>Recipient name</span><input value={name} onChange={e=>setName(e.target.value)} maxLength={30} placeholder="Enter name for personalisation"/></label>}<div className="quantity-row"><div className="quantity"><button onClick={()=>setQuantity(Math.max(1,quantity-1))} aria-label="Decrease quantity"><Minus/></button><span>{quantity}</span><button onClick={()=>setQuantity(Math.min(20,quantity+1))} aria-label="Increase quantity"><Plus/></button></div><button className="button button-primary grow" onClick={add}><ShoppingBag size={17}/>Add to cart</button><button className={`icon-button ${wishlist.includes(product.id)?'is-wished':''}`} onClick={()=>toggleWishlist(product.id)} aria-label="Toggle wishlist"><Heart fill={wishlist.includes(product.id)?'currentColor':'none'}/></button></div><button className="button buy-now" onClick={()=>{add();router.push('/checkout')}}><Zap size={17}/>Buy now</button></div>}
