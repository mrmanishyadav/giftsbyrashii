'use client';import{useState}from'react';
export function EnquiryForm({type}:{type:'corporate'|'contact'}){const[notice,setNotice]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget).entries());const response=await fetch(`/api/${type}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const data=await response.json() as {error?:string};setNotice(response.ok?'Thank you. Our gifting specialist will be in touch.':data.error??'Please try again.');if(response.ok)e.currentTarget.reset()}return <form className="enquiry-form" onSubmit={submit}>{type==='corporate'?<><label className="field"><span>Company</span><input name="company" required/></label><label className="field"><span>Contact person</span><input name="contactPerson" required/></label><label className="field"><span>Work email</span><input name="email" type="email" required/></label><label className="field"><span>Phone</span><input name="phone" required/></label><label className="field"><span>Approx. quantity</span><input name="quantity" type="number" min="1" required/></label><label className="field"><span>Budget per gift (₹)</span><input name="budget" type="number" min="1" required/></label><label className="field"><span>Occasion</span><input name="occasion" required/></label><label className="field"><span>Delivery location</span><input name="deliveryLocation" required/></label><label className="field wide"><span>Tell us what you have in mind</span><textarea name="requirements" rows={5} required/></label></>:<><label className="field"><span>Name</span><input name="name" required/></label><label className="field"><span>Email</span><input name="email" type="email" required/></label><label className="field"><span>Phone</span><input name="phone"/></label><label className="field"><span>Subject</span><input name="subject" required/></label><label className="field wide"><span>Message</span><textarea name="message" rows={6} required/></label></>}<button className="button button-primary">{type==='corporate'?'Plan my gifting':'Send message'} →</button>{notice&&<p className="form-notice" role="status">{notice}</p>}</form>}
