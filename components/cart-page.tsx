'use client';
import Link from'next/link';import{Gift,Minus,Plus,ShoppingBag,Trash2}from'lucide-react';import{useCart}from'./cart-provider';import{calculateTotals,FREE_SHIPPING_THRESHOLD}from'@/lib/pricing';
export function CartPage(){const{items,remove,setQuantity}=useCart();const totals=calculateTotals(items.map(i=>({id:i.id,price:i.price,mrp:i.mrp,quantity:i.quantity})));if(!items.length)return <div className="empty-state cart-empty"><ShoppingBag/><h1>Your gift bag is waiting</h1><p>Beautiful surprises are just a few thoughtful clicks away.</p><Link href="/shop" className="button button-primary">Explore gifts</Link></div>;return <main className="cart-page shell"><div className="cart-title"><h1>Your gift bag</h1><span>{items.length} {items.length===1?'gift':'gifts'}</span></div><div className="cart-layout"><section><div className="shipping-progress"><div><b>{totals.shipping===0?'You unlocked free shipping!':`Add ₹${Math.max(0,FREE_SHIPPING_THRESHOLD-totals.subtotal)} more for free shipping`}</b><span>{totals.shipping===0?'A little more joy, on us.':'You’re almost there.'}</span></div><div><i style={{width:`${Math.min(100,totals.subtotal/FREE_SHIPPING_THRESHOLD*100)}%`}}/></div></div>{items.map(item=><article className="cart-item" key={item.id}><div className="cart-art" style={{background:item.color}}><Gift/></div><div><Link href={`/product/${item.slug}`}><h2>{item.name}</h2></Link>{item.personalization&&<p>Personalised: {Object.values(item.personalization).join(', ')}</p>}{item.hamper&&<p>{item.hamper.items.length} hamper items · {item.hamper.packaging}</p>}<span>₹{item.price.toLocaleString('en-IN')} <s>₹{item.mrp.toLocaleString('en-IN')}</s></span><div className="quantity"><button onClick={()=>setQuantity(item.id,item.quantity-1)}><Minus/></button><span>{item.quantity}</span><button onClick={()=>setQuantity(item.id,item.quantity+1)}><Plus/></button></div></div><button className="remove" onClick={()=>remove(item.id)} aria-label={`Remove ${item.name}`}><Trash2/></button></article>)}</section><aside className="order-summary"><h2>Order summary</h2><div><span>Subtotal</span><b>₹{totals.subtotal.toLocaleString('en-IN')}</b></div><div><span>Product savings</span><b className="saving">−₹{totals.productSavings.toLocaleString('en-IN')}</b></div><div><span>Shipping</span><b>{totals.shipping?'₹99':'Free'}</b></div><label>Coupon code<div><input placeholder="Enter code"/><button>Apply</button></div></label><div className="total"><span>Estimated total<small>Inclusive of taxes</small></span><b>₹{totals.total.toLocaleString('en-IN')}</b></div><Link href="/checkout" className="button button-primary">Continue to checkout →</Link><p>Secure checkout · Server-verified pricing</p></aside></div></main>}
