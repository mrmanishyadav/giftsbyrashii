'use client';import{useState}from'react';const statuses=['confirmed','processing','personalized','packed','shipped','out_for_delivery','delivered','cancelled','return_requested','returned','refund_pending','refunded'];export function AdminOrderActions({orderId,current}:{orderId:string;current:string}){const[status,setStatus]=useState(current);const[notice,setNotice]=useState('');async function save(){const response=await fetch('/api/admin/orders',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify({orderId,status})});setNotice(response.ok?'Order timeline updated.':'Update failed.')}return <section className="admin-card status-control"><div><h2>Update fulfilment</h2><p>Customers see every saved state in their order timeline.</p></div><select value={status} onChange={e=>setStatus(e.target.value)}>{statuses.map(s=><option key={s} value={s}>{s.replaceAll('_',' ')}</option>)}</select><button className="button button-primary" onClick={save}>Save status</button>{notice&&<span>{notice}</span>}</section>}
