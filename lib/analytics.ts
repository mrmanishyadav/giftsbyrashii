export type AnalyticsEvent='product_view'|'search'|'add_to_cart'|'remove_from_cart'|'begin_checkout'|'coupon_apply'|'purchase'|'wishlist'|'hamper_started'|'hamper_completed';export function track(event:AnalyticsEvent,properties:Record<string,string|number|boolean>={}){if(typeof window==='undefined')return;window.dispatchEvent(new CustomEvent('giftmitra:analytics',{detail:{event,properties,timestamp:Date.now()}}));}
